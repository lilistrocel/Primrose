# Staging Environment Configuration Template
# Copy this content to your actual .env.staging file

# Server Configuration
PORT=3000
NODE_ENV=staging

# JWT Configuration - Staging secrets (different from production)
JWT_SECRET=staging_jwt_secret_32_characters_minimum_length_required
JWT_EXPIRES_IN=24h

# Security
BCRYPT_ROUNDS=10

# CORS - Staging URLs
ALLOWED_ORIGINS=https://staging.yourdomain.com,https://staging-api.yourdomain.com,http://localhost:3000

# Database - Staging MongoDB
MONGODB_URI=mongodb://primrose_user:${MONGODB_PASSWORD}@mongodb:27017/primrose_staging?authSource=primrose_staging
MONGODB_PASSWORD=staging_db_password_change_this
MONGODB_ADMIN_PASSWORD=staging_admin_password_change_this

# Staging Flags
DOCKER_MONGODB=true
SKIP_DB=false
ALLOW_NO_DB=false

# Monitoring
GRAFANA_ADMIN_PASSWORD=staging_grafana_password

# Staging Notes:
# 1. Use different secrets from production
# 2. Can use weaker passwords for testing
# 3. Enable verbose logging for debugging
# 4. Use staging domain names
# 5. Monitor performance and errors 