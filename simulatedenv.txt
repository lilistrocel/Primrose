# Simulated .env File Configuration
# Copy this content to your actual .env file

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=b4c875e142d8407e333587b2c4fcb0aa24c6f6d00bfa48315353654a5fd986ce
JWT_EXPIRES_IN=7d

# Security
BCRYPT_ROUNDS=12

# CORS - Frontend URLs
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:3001

# Docker Configuration
DOCKER_MONGODB=true

# Database - Docker MongoDB with Authentication
MONGODB_URI=mongodb://primrose_user:primrose_password@localhost:27017/primrose

# Development Options (optional - for testing without DB)
# SKIP_DB=false
# ALLOW_NO_DB=false

# Additional Notes:
# - Make sure Docker Desktop is running
# - MongoDB will be available on localhost:27017
# - MongoDB Express UI will be on http://localhost:8081
# - Database name: primrose
# - App user: primrose_user / primrose_password
# - Admin user: admin / admin123 