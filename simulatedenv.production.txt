# Production Environment Configuration Template
# Copy this content to your actual .env.production file

# Server Configuration
PORT=3000
NODE_ENV=production

# JWT Configuration - USE STRONG PRODUCTION SECRETS
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_256_BIT_SECRET_IN_PRODUCTION
JWT_EXPIRES_IN=7d

# Security
BCRYPT_ROUNDS=12

# CORS - Production URLs only
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://api.yourdomain.com

# Database - Production MongoDB
MONGODB_URI=mongodb://primrose_user:${MONGODB_PASSWORD}@mongodb:27017/primrose?authSource=primrose
MONGODB_PASSWORD=CHANGE_THIS_TO_STRONG_DB_PASSWORD
MONGODB_ADMIN_PASSWORD=CHANGE_THIS_TO_STRONG_ADMIN_PASSWORD

# Production Flags
DOCKER_MONGODB=true
SKIP_DB=false
ALLOW_NO_DB=false

# Monitoring (Optional)
GRAFANA_ADMIN_PASSWORD=CHANGE_THIS_TO_STRONG_GRAFANA_PASSWORD

# Production Notes:
# 1. Generate strong passwords using: openssl rand -base64 32
# 2. JWT_SECRET should be 256-bit: openssl rand -hex 32
# 3. Never commit this file to version control
# 4. Use environment variable injection in production
# 5. Consider using AWS Secrets Manager or similar for secrets
# 6. Enable monitoring profile: docker-compose --profile monitoring up 